const { MessageEmbed } = require('discord.js')
module.exports = {
    name: "guild",
    aliases: ["guildid"],
    description: "Get info on a guild!",
    async run(client, message, args){
        
    }
}